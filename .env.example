# No environment variables are required for a fully static Netlify deploy.
# Netlify Forms are configured via the HTML data-netlify attribute — no API keys needed.
#
# If you later add server-side features, document them here:
# EXAMPLE_VAR=
